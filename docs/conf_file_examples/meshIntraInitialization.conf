# =============================================================================
#  MC-DC Simulator -- configuration file
# -----------------------------------------------------------------------------
#  UNITS: by default this file is in STANDARD UNITS (SI): metres, seconds, Tesla.
#  Values are scaled silently to the internal working units (mm, ms) on load.
#  Set  use_mm_ms 1  to declare the file is ALREADY in internal units (mm, ms);
#  then nothing is scaled (this also applies to the scheme file).
#
#    duration ......................... seconds        [s]
#    diffusivity / d_intra / d_extra .. metre^2/second  [m^2/s]
#    t2_intra / t2_extra .............. seconds        [s]
#    <voxels> / <sampling_area> /
#      <delta> / coll_sphere_dist /
#      hex radius / separation ........ metres         [m]
#    permeability (kappa) ............. metre/second   [m/s]   (see exceptions)
#    scheme file (G, Delta, delta, TE)  Tesla/m, s     [T/m, s]
#
#  EXCEPTIONS (not standard-unit scaled):
#    * permeability kappa is a VELOCITY: m/s equals mm/ms numerically, so it is
#      scale-invariant (the same value works in either mode).
#    * gamma-packing parameters (alpha, beta, min_radius) use their own
#      micrometre (um) convention and are NOT scaled.
#    * geometry-file scales (ply_scale; first line of a cylinder/sphere list) are
#      an independent file->mm scaling, unaffected by use_mm_ms.
#
#  Lines beginning with '#' are comments and are ignored.
# =============================================================================

N 1000
T 100
duration 0.020
diffusivity 0.6e-10
exp_prefix instructions/demos/output/mesh_initialization_test

scheme_file docs/scheme_files/PGSE_sample_scheme.scheme
write_txt 1
write_bin 0
write_traj_file 1

<obstacle>
ply instructions/meshes/PorusMedia.ply
ply_scale 0.001
</obstacle>

<voxels>
-1.0e-5 -1.0e-5 -5.0e-5
 1.0e-5  1.0e-5  5.0e-5
</voxels>

<spawning_area>
-5.0e-6 -5.0e-6 -5.0e-6
 5.0e-6  5.0e-6  5.0e-6
</spawning_area>

ini_walkers_pos intra

num_process 1
<END>
