"""MC/DC Simulator -- Python launcher for the compiled C++ binary.

The simulator itself is a C++/CMake program (`MC-DC_Simulator`). This thin package
builds it during `pip install` and exposes it as the `mcdc` console command, so it
can be installed and run the way Python tools are.
"""
__version__ = "2.2.0"
